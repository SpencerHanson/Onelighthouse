== README

The set-up is extremely straight forward

* Clone the repo

* Run a <tt>bundle install</tt>

* Seed the db with some fake data with <tt>rake db:seed</tt>

* Run the server with <tt>rails s</tt>

* Navigate to http://localhost:3000/ in your browser

* You're good to go!



